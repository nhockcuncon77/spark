# Backend API base URL (no trailing slash). Used by login/register pages.
# Default is already set in code; override here or in Render env.
VITE_API_URL=https://spark-c6bk.onrender.com

# Optional: Expo web app URL after login (default: /app/)
# VITE_SPARK_APP_URL=/app/
